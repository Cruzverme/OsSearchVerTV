<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorVertvBackground</item>
        <item name="colorPrimaryDark">@color/colorVertvBackgroundDark</item>
        <item name="colorAccent">@color/colorVertvOrange</item>
    </style>

    <style name="VertvFont" parent="Theme.AppCompat">
        <item name="android:fontFamily">@font/arlrdbd</item>
        <item name="android:textColor">@color/colorVertvLetters</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textColorHint">@color/colorVertvLetters</item>
        <item name="colorAccent">@color/colorVertvOrange</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
